# Dependencias mínimas para CI (GitHub Actions).
# Objetivo: instalación rápida para correr ruff + smoke tests + tests de la API.
# Para desarrollo local usar environment.yml (conda).

ruff==0.6.9
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3
joblib==1.4.2
pytest==8.3.5

# Tests de la API (S5): TestClient de FastAPI + contratos + logging.
fastapi==0.115.0
httpx==0.27.2
pydantic==2.9.0
loguru==0.7.2

# Prometheus
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.21.0