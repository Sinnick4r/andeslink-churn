# requirements.txt  runtime de la API de inferencia (Dockerfile principal).
# Versiones EXACTAS (==)
# Corre `pip audit` antes de cada entrega. Solo lo que el runtime importa

# scikit-learn pinneado EXACTO: el .joblib es version-sensitive
scikit-learn==1.5.2

# Web framework + server ASGI.
fastapi==0.115.0
uvicorn==0.32.0

# Contratos y validación.
pydantic==2.9.0

# Datos / feature engineering en serving (add_derived_features) e inferencia.
numpy==1.26.4
pandas==2.2.3

# Carga del artefacto serializado.
joblib==1.4.2

# Logging estructurado.
loguru==0.7.2

# Prometheus
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.21.0